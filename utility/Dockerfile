FROM node:14-alpine

WORKDIR /app

#로컬 폴더에 미러링하여 컨테이너에서 생성한 것들을 호스트 머신에서도 사용할 수 있게 함.
#컨테이너 도움으로 호스트 머신에서 프로젝틀르 생성할 수 있다. 이것이 유틸리티 컨테이너의 배경에 깔린 아이디어이다.
#호스트 머신에 모든 부가 도구를 설치하지 않고도 호스트 머신에 영향을 미치는 어떤 것을 실행하는데 유틸리티 컨테이너를 사용할 수 있따.
# package.json 파일 생성됨 - 바운드마운트 사용
# 이렇게 되면 로컬 컴퓨터에 부가 도구들을 설치할 필요가 없다.
# npm init 대신 npm install 사용 왜? 부가 명령어 사용할 수 있도록?
# 바운드 마운트를 사용한 컨테이너가 다운되면 로컬 호스트 머신의 파일, 폴더들은 어떻게?

# CMD 의 경우 'docker run' + 이미지 이름 뒤의 명령어로 덮어씌어지지만 ENTRYPOINT의 경우 덮어 씌어지지 않고 실행 후 이미지 이름 뒤 명령어가 실행된다.
ENTRYPOINT [ "executable" ]